#! /usr/bin/env bash

if [ -z $PROJECT ]; then
    echo "Active virtual session not found. See docs."
    exit
fi

rm -f $PROJECT/bin/camera
pyinstaller --clean --noconfirm --name camera $PROJECT/src/app.py
cp $PROJECT/dist/$PROJ_NAME/$PROJ_NAME $PROJECT/bin/$PROJ_NAME

