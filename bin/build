#! /usr/bin/env bash

if [ -z $PROJECT ]; then
    echo "Active virtual session not found. See docs."
    exit
fi

rm -f $PROJECT/bin/camera
pyinstaller $PROJECT/src/camera.py
ln -s $PROJECT/dist/$PROJ_NAME/$PROJ_NAME $PROJECT/bin/$PROJ_NAME

